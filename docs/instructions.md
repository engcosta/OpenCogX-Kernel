🧬 المعمارية المتطورة: Self-Evolving Hybrid Graph RAG

هذا النظام لم يعد مجرد "محرك بحث"، بل أصبح كائناً معرفياً ينمو ذاتياً. يتكون من 4 مراحل رئيسية:
🏗️ المرحلة 1: التأسيس وبناء المعرفة (Offline Ingestion)

الهدف: تحويل البيانات الخام إلى "عقل" مهيكل.

    المعالجة المتقدمة (Hierarchical Processing):

        تقسيم الملفات إلى Parent Chunks (سياق كبير) و Child Chunks (للبحث الدقيق).

    المسار المزدوج (Dual Indexing):

        Vector Path: تخزين النصوص في Qdrant.

        Graph Path:

            استخدام GLiNER لاستخراج الكيانات بدقة عالية.

            استخدام Llama-3.3 لاستخراج العلاقات بناءً على Ontology صارمة (مثل: CAUSES, PART_OF, CONTRADICTS).

            تخزين النتيجة في Neo4j.

🏋️ المرحلة 2: التدريب الاستباقي (Proactive "Dr. Zero" Loop) - ✨ الجديد

الهدف: اكتشاف الفجوات المعرفية وسدها قبل وصول المستخدم.

تحدث هذه العملية فوراً بعد رفع الملفات (Training Camp):

    The Proposer (المحرض):

        يقوم Llama-3.3 بفحص الـ Graph وتوليد "أسئلة معقدة" (Complex Queries) تتطلب ربط علاقات بعيدة.

        مثال: "ما العلاقة غير المباشرة بين المفهوم A والمفهوم C؟"

    The Solver (نظامك):

        يحاول الإجابة باستخدام المعرفة الحالية.

    The Critic (المقيم):

        يقيم الإجابة. إذا فشل النظام (Fail)، يتم فوراً تفعيل Research Agent للبحث عن المعلومة الناقصة وإضافتها للجراف.

        النتيجة: جراف قوي ومترابط جاهز لأصعب الأسئلة.

🚀 المرحلة 3: الدورة السريعة للمستخدم (Online Fast Loop)

الهدف: الرد على المستخدم في < 3 ثوانٍ.

    تحليل النية (Intent Routing):

        هل السؤال تعريفي؟ (بحث Vector فقط).

        هل السؤال استنتاجي؟ (بحث Graph + Vector).

    الاسترجاع الهجين (Hybrid Retrieval):

        Qdrant: جلب أفضل 5 فقرات نصية.

        Neo4j: جلب الكيانات المذكورة + جيرانها (2-hop Subgraph).

    الدمج والتوليد (Context Fusion):

        دمج النصوص والعلاقات في سياق واحد.

        توليد الإجابة باستخدام Llama-3.3.

    الرد الفوري: إرسال الإجابة للمستخدم.

🧠 المرحلة 4: الدورة التفاعلية للتعلم (Reactive Learning Loop)

الهدف: التعلم من تفاعل المستخدم الحقيقي (في الخلفية).

    Async Trigger: بعد الرد، يتم إرسال المحادثة لـ Celery Worker.

    The Critic (مرة أخرى):

        يفحص: هل كانت الإجابة مقنعة؟ هل هناك هلوسة؟

    تحديث الذاكرة (Episodic Memory):

        نجاح (Pass): إنشاء رابط "اختصار" في الجراف: (Question) -> [SOLVED_BY] -> (Answer). في المرة القادمة، الإجابة ستكون فورية (Cache).

        فشل (Fail): تسجيل "عقدة فشل". إذا تكرر الفشل في نفس الموضوع، ينطلق Research Agent لترميم المعرفة.

🛠️ التحديث التقني (Tech Stack V3.0)
المكون	التقنية المختارة	السبب
Orchestration	n8n (Primary) + Python Scripts	لسهولة بناء وتعديل الـ 3 تدفقات بصرياً.
LLM Brain	Llama-3.3-70B (via Groq)	أذكى وأسرع نموذج مفتوح حالياً، ويدعم JSON Mode بامتياز.
Graph Logic	GLiNER + Neo4j	GLiNER لا يتطلب تدريب لاستخراج الكيانات، و Neo4j هو المعيار الذهبي.
Vector DB	Qdrant	سريع جداً ويدعم Metadata Filtering المعقد.
Queuing	RabbitMQ / Redis	لفصل "المسار السريع" عن "مسار التعلم".

هذا المنهج يجمع بين قوة RAG التقليدية، وذكاء Graphs، ومنهجية Self-Evolution الحديثة، مما يجعله نظاماً قوياً جداً وقابلاً للتطبيق الفعلي.


